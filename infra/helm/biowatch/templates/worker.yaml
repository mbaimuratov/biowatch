apiVersion: v1
kind: Service
metadata:
  name: {{ include "biowatch.fullname" . }}-worker
  labels:
    {{- include "biowatch.componentLabels" (dict "root" . "component" "worker") | nindent 4 }}
  annotations:
    {{- if .Values.observability.prometheus.scrape }}
    prometheus.io/scrape: "true"
    prometheus.io/path: /metrics
    prometheus.io/port: {{ .Values.worker.metrics.port | quote }}
    {{- end }}
    {{- with .Values.worker.metrics.service.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ports:
    - name: metrics
      port: {{ .Values.worker.metrics.port }}
      targetPort: metrics
  selector:
    {{- include "biowatch.selectorLabels" (dict "root" . "component" "worker") | nindent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "biowatch.fullname" . }}-worker
  labels:
    {{- include "biowatch.componentLabels" (dict "root" . "component" "worker") | nindent 4 }}
spec:
  replicas: {{ .Values.worker.replicaCount }}
  selector:
    matchLabels:
      {{- include "biowatch.selectorLabels" (dict "root" . "component" "worker") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "biowatch.selectorLabels" (dict "root" . "component" "worker") | nindent 8 }}
    spec:
      serviceAccountName: {{ include "biowatch.serviceAccountName" (dict "root" . "component" "worker") }}
      containers:
        - name: worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - python
            - -m
            - app.jobs.worker
          ports:
            - name: metrics
              containerPort: {{ .Values.worker.metrics.port }}
          env:
            {{- include "biowatch.appEnv" . | nindent 12 }}
            {{- include "biowatch.telegramTokenEnv" . | nindent 12 }}
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: biowatch-root-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/mbaimuratov/biowatch.git
    targetRevision: main
    path: infra/gitops/apps/dev
    directory:
      recurse: true

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

# Generate this file with kubeseal for the biowatch-dev namespace before
# applying the dev environment. Do not copy the prod SealedSecret because
# Sealed Secrets are scoped to the encrypted namespace/name.
#
# Expected Secret name: biowatch-secret
# Expected namespace: biowatch-dev
